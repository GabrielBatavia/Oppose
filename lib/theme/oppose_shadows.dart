import 'package:flutter/material.dart';

class OpposeShadows {
  const OpposeShadows._();

  static List<BoxShadow> get card => const [
    BoxShadow(color: Color(0x1F6D4A2E), blurRadius: 18, offset: Offset(0, 8)),
  ];
}
